version: "3.8"

services:
  sveltgui:
    build:
      context: .
      args:
        VITE_API_BASE: "/analyze"
    environment:
      DOWNSTREAM_URL: ""      # e.g. http://downstream:9000
      LOG_LEVEL: "info"
    ports:
      - "8080:8080"
    restart: unless-stopped