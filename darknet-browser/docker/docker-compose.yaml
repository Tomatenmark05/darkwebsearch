version: "3.8"

services:
  sveltgui:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        VITE_API_BASE: "/analyze"
    environment:
      DOWNSTREAM_URL: ""
      LOG_LEVEL: "info"
    ports:
      - "8080:8080"
    restart: unless-stopped